# The path segment after /call/ is the OutLayer project (owner/name), not the GitHub repo.
# It is a namespace of its own: the project is still called vrf-ark even though this
# repository was renamed to out-layer/vrf-example.

curl -X POST https://testnet-api.outlayer.ai/call/zavodil2.testnet/vrf-ark \
  -H "X-Payment-Key: owner:nonce:secret" \
  -H "Content-Type: application/json" \
  -d '{
    "input": {"seed": "test-coin", "max": 1},
    "resource_limits": {
      "max_instructions": 10000000000,
      "max_memory_mb": 128,
      "max_execution_seconds": 60
    }
  }'